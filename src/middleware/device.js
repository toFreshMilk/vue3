export default async function ({ redirect, $device, $route }) {
    // const isProduction = process.env.NODE_ENV === 'production'
    // if (isProduction) {
    //     if ($device.isMobile) {
    //         return redirect('http://www.z-inmall.com/w/main.zin')
    //     } else if ($device.isDesktop) {
    //         return redirect('http://www.z-inmall.com/w/main.zin')
    //     }
    // }
}